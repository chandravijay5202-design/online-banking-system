# Only needed if the frontend is deployed separately from the backend
# (Option B in DEPLOYMENT.md). Point it at your deployed backend's /api path.
# VITE_API_BASE_URL=https://your-backend.up.railway.app/api
